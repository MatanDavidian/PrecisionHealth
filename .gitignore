node_modules/
dist/
.DS_Store
*.local
.env
.env.*
!.env.example
*.tsbuildinfo

# Supabase project credentials
.env.local

# Supabase CLI scratch state, per machine
supabase/.temp/

# Connect IQ build output
garmin/bin/

# Developer signing keys — private keys, never in the repo.
# The Connect IQ key normally lives at ~/.Garmin/developer_key.
*developer_key*
*.der
*.pem

# Playwright
test-results/
playwright-report/
blob-report/

# Garmin: the device credential, and the source generated from it
garmin/local.env
garmin/source/BuildConfig.mc
